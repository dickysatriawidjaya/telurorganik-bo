import request from '@/utils/request';
import Resource from '@/api/resource';

class ItemResource extends Resource {
  constructor() {
    super('items');
  }

  permissions(id) {
    return request({
      url: '/' + this.uri + '/' + id + '/permissions',
      method: 'get',
    });
  }

  updatePermission(id, permissions) {
    return request({
      url: '/' + this.uri + '/' + id + '/permissions',
      method: 'put',
      data: permissions,
    });
  }
}

export { ItemResource as default };
